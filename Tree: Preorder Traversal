void preOrder(Node *root)
     { 
         int data;
         cin>>data;
         if(root == NULL) 
            return;
        cout<<root->data<<" ";
        preOrder(root->left);
        preOrder(root->right);
    }
